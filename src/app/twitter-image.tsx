// Re-uses the OpenGraph image for Twitter/X cards. Twitter prefers 1200x630
// for summary_large_image cards, which matches our OG size.
export { default } from "./opengraph-image";
export { runtime, alt, size, contentType } from "./opengraph-image";
